def check_positive_number():
    """
    20-нұсқа: Енгізілген мәннің оң сан екенін тексеріп, 
    теріс болса ValueError туғызатын код
    """
    try:
        # Пайдаланушыдан сан енгізуін сұрау
        number = float(input("Бір сан енгізіңіз: "))
        
        # Сандың оң болуын тексеру
        if number < 0:
            raise ValueError("Теріс сан енгіздіңіз! Тек оң сандар қабылданады.")
        
        # Егер сан оң болса
        print(f"✅ Сіз оң сан енгіздіңіз: {number}")
        return number
        
    except ValueError as e:
        # Егер сан емес мән енгізілсе немесе теріс сан болса
        if "Теріс сан" in str(e):
            print(f"❌ Қате: {e}")
        else:
            print("❌ Қате: Сан енгізіңіз!")
        return None

def main():
    print("=== Оң сандарды тексеру бағдарламасы ===\n")
    
    while True:
        result = check_positive_number()
        
        if result is not None:
            print(f"🎉 {result} сан дұрыс қабылданды!\n")
        else:
            print("🔄 Қайтадан көрейік...\n")
        
        # Бағдарламаны жалғастыруды сұрау
        continue_choice = input("Тағы сан тексергіңіз келе ме? (иә/жоқ): ").lower()
        if continue_choice not in ['иә', 'иа', 'yes', 'y', '']:
            print("👋 Бағдарлама аяқталды!")
            break

if __name__ == "__main__":
    main()